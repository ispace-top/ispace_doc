[site]
debug = False
# 允许访问的域名，多个域名用英文逗号分隔，生产环境请设置为实际域名
allowed_hosts = localhost,127.0.0.1

[database]
engine = postgresql
name = ispace
user = ispace
# 密码通过环境变量 DB_PASSWORD 注入（docker-compose.yml 中配置）
# 本地直接运行时修改此值为实际密码
password =
host = db
port = 5432

[session]
cookie_age = 604800
browser_close = false
cookie_secure = true
cookie_httponly = true

[locale]
language = zh-hans
timezone = Asia/Shanghai

[selenium]
# Docker 环境使用 Chromium headless
# driver = Chrome

# ============================================================
# 任务队列 (Celery + Redis)
# ============================================================
[cache]
backend = redis
location = redis://redis:6379/0

[celery]
broker_url = redis://redis:6379/1
result_backend = redis://redis:6379/2

# ============================================================
# 搜索引擎
# ============================================================
[search]
backend = elasticsearch

[search.elasticsearch]
hosts = http://elasticsearch:9200
index = ispace_docs

# ============================================================
# 文件存储
# ============================================================
[storage]
backend = local

# ============================================================
# 邮件
# ============================================================
[email]
smtp_host =
smtp_port = 587
smtp_user =
smtp_password =
smtp_from = noreply@ispace.com

# ============================================================
# 第三方认证（按需取消注释）
# ============================================================
# [auth.oidc]
# discovery_url =
# client_id =
# client_secret =

# [auth.wecom]
# corp_id =
# corp_secret =
# agent_id =

# [auth.dingtalk]
# app_id =
# app_secret =
